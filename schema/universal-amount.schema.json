{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:uas:spec:0.0.1:universal-amount.schema.json",
  "title": "UniversalAmount",
  "description": "Exact UAS amount. Currency is supplied by surrounding context.",
  "type": "object",
  "required": ["amount"],
  "additionalProperties": false,
  "properties": {
    "amount": {
      "type": "string",
      "pattern": "^-?(0|[1-9][0-9]*)\\.[0-9]{8}$",
      "description": "Signed fixed-point decimal normalized to eight fractional places."
    }
  }
}
